# SIS-253 — /esg · Fale com a Gente! igual à referência falecomagente.png (+ ponto na linha, hover botão)

- **Data da exportação:** 2026-09-15T20:51:00.000Z
- **Modo:** somente leitura (`get_issue` + `list_comments`); nada foi alterado no Linear
- **Origem Linear:** https://linear.app/sistran-labs/issue/SIS-253/esg-fale-com-a-gente-igual-a-referencia-falecomagentepng-ponto-na
- **get_issue:** includeRelations=true, includeCustomerNeeds=true, includeReleases=true
- **list_comments:** limit=250, orderBy=createdAt

## Metadados

```json
{
  "id": "SIS-253",
  "uuid": "3d43b2d3-0299-4332-ac39-72df554cb0fb",
  "title": "/esg · Fale com a Gente! igual à referência falecomagente.png (+ ponto na linha, hover botão)",
  "priority": {
    "value": 0,
    "name": "No priority"
  },
  "url": "https://linear.app/sistran-labs/issue/SIS-253/esg-fale-com-a-gente-igual-a-referencia-falecomagentepng-ponto-na",
  "gitBranchName": "meduardamcp/sis-253-esg-fale-com-a-gente-igual-a-referencia-falecomagentepng",
  "createdAt": "2026-09-14T15:35:26.248Z",
  "updatedAt": "2026-09-15T20:33:55.968Z",
  "archivedAt": null,
  "completedAt": null,
  "startedAt": "2026-09-14T15:44:06.804Z",
  "canceledAt": null,
  "dueDate": null,
  "slaStartedAt": null,
  "slaMediumRiskAt": null,
  "slaHighRiskAt": null,
  "slaBreachesAt": null,
  "status": "Entregue",
  "statusType": "started",
  "labels": [
    "conferir"
  ],
  "createdBy": "Maria Eduarda M Camargo",
  "createdById": "d540a312-a721-479d-9da4-3b86ed7868ba",
  "team": "Sistran Labs",
  "teamId": "b98f204c-dea3-41e6-99c3-c0347bfb0454",
  "attachments": [
    {
      "id": "6f4a7329-7c37-4ae8-9312-bf95fd256467",
      "title": "Referência visual — Fale com a Gente!",
      "subtitle": null,
      "url": "https://uploads.linear.app/5ee948d4-4128-4d5f-8661-8bdc047db37b/289bdf77-19ee-4044-89a7-5e5a6749f3a3/b5c37fd5-324b-41f5-b08d-765fdeec531f?signature=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJwYXRoIjoiLzVlZTk0OGQ0LTQxMjgtNGQ1Zi04NjYxLThiZGMwNDdkYjM3Yi8yODliZGY3Ny0xOWVlLTQwNDQtODlhNy01ZTVhNjc0OWYzYTMvYjVjMzdmZDUtMzI0Yi00MWY1LWIwOGQtNzY1ZmRlZWM1MzFmIiwiaWF0IjoxNzg5NTA2MDY4LCJleHAiOjE3ODk1MDYzNjh9.pzsrrGE8RvX6srBbofUC5jYk3X-3vnlRM4pqSIA4fAc"
    }
  ],
  "documents": [],
  "stateHistory": [
    {
      "state": {
        "id": "bc5681b9-61b8-47a5-b698-88ac72996d2e",
        "name": "Backlog",
        "type": "backlog"
      },
      "startedAt": "2026-09-14T15:35:26.248Z",
      "endedAt": "2026-09-14T15:35:36.532Z"
    },
    {
      "state": {
        "id": "cedc27f4-1573-413a-94ea-6faf080ffe19",
        "name": "Todo",
        "type": "unstarted"
      },
      "startedAt": "2026-09-14T15:35:36.532Z",
      "endedAt": "2026-09-14T15:44:06.821Z"
    },
    {
      "state": {
        "id": "5f40aa5d-3589-452e-8bff-8f2539a4fd24",
        "name": "In Progress",
        "type": "started"
      },
      "startedAt": "2026-09-14T15:44:06.821Z",
      "endedAt": "2026-09-14T16:28:49.528Z"
    },
    {
      "state": {
        "id": "c172d2db-acea-4327-b3f6-b72de70efe6a",
        "name": "In Review",
        "type": "started"
      },
      "startedAt": "2026-09-14T16:28:49.528Z",
      "endedAt": "2026-09-14T17:56:19.012Z"
    },
    {
      "state": {
        "id": "5f40aa5d-3589-452e-8bff-8f2539a4fd24",
        "name": "In Progress",
        "type": "started"
      },
      "startedAt": "2026-09-14T17:56:19.012Z",
      "endedAt": "2026-09-14T18:09:19.170Z"
    },
    {
      "state": {
        "id": "c172d2db-acea-4327-b3f6-b72de70efe6a",
        "name": "In Review",
        "type": "started"
      },
      "startedAt": "2026-09-14T18:09:19.170Z",
      "endedAt": "2026-09-14T18:36:41.517Z"
    },
    {
      "state": {
        "id": "5f40aa5d-3589-452e-8bff-8f2539a4fd24",
        "name": "In Progress",
        "type": "started"
      },
      "startedAt": "2026-09-14T18:36:41.517Z",
      "endedAt": "2026-09-14T18:45:09.203Z"
    },
    {
      "state": {
        "id": "c172d2db-acea-4327-b3f6-b72de70efe6a",
        "name": "In Review",
        "type": "started"
      },
      "startedAt": "2026-09-14T18:45:09.203Z",
      "endedAt": "2026-09-14T18:50:19.385Z"
    },
    {
      "state": {
        "id": "5f40aa5d-3589-452e-8bff-8f2539a4fd24",
        "name": "In Progress",
        "type": "started"
      },
      "startedAt": "2026-09-14T18:50:19.385Z",
      "endedAt": "2026-09-14T19:01:53.347Z"
    },
    {
      "state": {
        "id": "c172d2db-acea-4327-b3f6-b72de70efe6a",
        "name": "In Review",
        "type": "started"
      },
      "startedAt": "2026-09-14T19:01:53.347Z",
      "endedAt": "2026-09-15T20:33:55.958Z"
    },
    {
      "state": {
        "id": "dbb9dfbe-2cc4-423a-862f-94ca7705f813",
        "name": "Entregue",
        "type": "started"
      },
      "startedAt": "2026-09-15T20:33:55.958Z",
      "endedAt": null
    }
  ]
}
```

## Descrição original

## Pedido

Em `/esg`, a seção **Fale com a Gente!** deve ficar **igual à referência** `public/imagensexemplo/falecomagente.png` (anexa / no repo).

Motion pedido (texto da arte):

1. Ao **surgir no scroll**, o **ponto luminoso** percorre a **linha** ciano (do nó esquerdo até “conectar” no botão).
2. No **hover** do botão: o botão **avança alguns pixels** (para a frente / direita).
3. No mesmo hover: o **círculo ciano** (com a seta, no fim do botão) **gira suavemente**.

## Referência visual (o que o layout tem que bater)

* Fundo da seção: azul claro suave / ondas (não navy chapado atrás do cartão).
* Cartão em **vidro** (frosted glass branco translúcido), cantos bem arredondados — texto **escuro** (título + parágrafo), não branco sobre navy.
* **Linha ciano** com nó luminoso: horizontal no topo do texto, depois dobra em diagonal até o botão.
* Botão pílula branco “Fale com a SISTRAN” + **círculo ciano** com seta branca.
* À direita: blob azul escuro com **logo Sistran** (selo branco), **órbitas ciano** com pontos, **dois balões de chat** em vidro com reticências.

Copy do bloco **não muda** (copy-lock): título, parágrafo e rótulo do botão como hoje em `ContactCTA`.

## Estado atual (auditado)

* Montagem em `src/app/esg/page.tsx`: `<ContactCTA haloClaro reativo contatoNoModal />`.
* Componente: `src/components/ContactCTA.tsx` — cartão com degradê **navy/ciano** inline, texto **branco**, botão branco com `ArrowRight` simples (sem círculo ciano), **sem** linha/ponto luminoso, **sem** blob/logo/órbitas/balões.
* CSS escopo ESG: `.cta-halo-claro` / `.cta-reativo` em `globals.css` (<issue id="c01a77a9-374f-4642-b768-4c42bd2b3aca" href="https://linear.app/sistran-labs/issue/SIS-142/esg-cta-fale-com-a-gente-fundo-azul-claro-cartao-mais-inovador-reacao">SIS-142</issue>) — halo atrás + levantamento do cartão; **não** é o layout da PNG.
* <issue id="c01a77a9-374f-4642-b768-4c42bd2b3aca" href="https://linear.app/sistran-labs/issue/SIS-142/esg-cta-fale-com-a-gente-fundo-azul-claro-cartao-mais-inovador-reacao">SIS-142</issue> (In Review) entregou halo + reação + modal; esta issue é **redesenho para a captura**, não reabrir flutuação genérica do cartão.

## O que fazer

1. Opt-in **só em** `/esg` (prop nova tipo `variante="esg-referencia"` / `layoutReferencia`) — as outras telas com `ContactCTA` **não** mudam.
2. Replicar composição da PNG (vidro + linha + blob/logo/órbitas/balões + botão com círculo ciano).
3. Motion: ponto percorre o path no `whileInView` / entrada; hover do botão → `translateX` leve + `rotate` no círculo ciano; `prefers-reduced-motion` / `data-motion=reduce` = estático (ponto no fim ou path completo sem animação; hover sem giro agressivo — cor/sombra ok).
4. Manter `contatoNoModal` (abre `ContactModal`).
5. Contraste AA no texto escuro sobre o vidro; capturas 1440 rest + hover + frame do scroll; lint OK.

## Fora de escopo

* Alterar `ContactCTA` nas outras rotas.
* Mudar copy.
* Cards ENVIRONMENT/GOVERNANCE (<issue id="37e82128-d8d6-493b-a363-98f21dd562ed" href="https://linear.app/sistran-labs/issue/SIS-252/esg-environment-governance-cards-azul-bem-clarinho-no-repouso-troca-de">SIS-252</issue>).
* Reabrir critérios genéricos da <issue id="c01a77a9-374f-4642-b768-4c42bd2b3aca" href="https://linear.app/sistran-labs/issue/SIS-142/esg-cta-fale-com-a-gente-fundo-azul-claro-cartao-mais-inovador-reacao">SIS-142</issue> (já In Review).

## Critérios de aceite

- [ ] Em `/esg`, o bloco bate visualmente com `public/imagensexemplo/falecomagente.png` (composição, tipografia de cor, grafismos).
- [ ] Ponto luminoso percorre a linha ao entrar no viewport (scroll).
- [ ] Hover: botão avança alguns px; círculo ciano gira suavemente.
- [ ] Reduce: sem percurso/giro contínuo; bloco legível.
- [ ] Outras páginas com `ContactCTA` intactas; modal de contato em `/esg` segue funcionando.
- [ ] Capturas; lint OK.

## Artefato

Referência: `public/imagensexemplo/falecomagente.png`

## Relações / Anexos / Releases / Customer needs

### Relações

```json
{
  "blocks": [],
  "blockedBy": [],
  "relatedTo": [
    {
      "id": "SIS-252",
      "title": "/esg · ENVIRONMENT + GOVERNANCE: cards azul bem clarinho no repouso + troca de cor no hover"
    },
    {
      "id": "SIS-142",
      "title": "/esg — CTA “Fale com a Gente!”: fundo azul claro, cartão mais inovador, reação ao mouse e o botão abrindo o modal de contato"
    },
    {
      "id": "SIS-276",
      "title": "/esg · Fale com a Gente!: grafismo cortado/baixo demais no rodapé"
    },
    {
      "id": "SIS-271",
      "title": "/esg · reveal on scroll (docs/scroll.md)"
    },
    {
      "id": "SIS-265",
      "title": "/esg · Fale com a Gente!: logo HD logosistranaltadefinicao.png"
    },
    {
      "id": "SIS-256",
      "title": "/contato · números: cards mais estreitos e sem contador 01 / 07"
    },
    {
      "id": "SIS-257",
      "title": "/esg · capa «ESG - Environment, Social & Governance» + seção intro com esg1.png"
    }
  ],
  "duplicateOf": null
}
```

### Anexos da issue

#### 6f4a7329-7c37-4ae8-9312-bf95fd256467

- title: Referência visual — Fale com a Gente!
- subtitle: null
- url (assinada, expira): https://uploads.linear.app/5ee948d4-4128-4d5f-8661-8bdc047db37b/289bdf77-19ee-4044-89a7-5e5a6749f3a3/b5c37fd5-324b-41f5-b08d-765fdeec531f?signature=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJwYXRoIjoiLzVlZTk0OGQ0LTQxMjgtNGQ1Zi04NjYxLThiZGMwNDdkYjM3Yi8yODliZGY3Ny0xOWVlLTQwNDQtODlhNy01ZTVhNjc0OWYzYTMvYjVjMzdmZDUtMzI0Yi00MWY1LWIwOGQtNzY1ZmRlZWM1MzFmIiwiaWF0IjoxNzg5NTA2MDY4LCJleHAiOjE3ODk1MDYzNjh9.pzsrrGE8RvX6srBbofUC5jYk3X-3vnlRM4pqSIA4fAc
- caminho local: `docs/arquivo-linear-entregue/anexos/SIS-253/6f4a7329-referencia-visual-fale-com-a-gente.png`
- bytes: 1269175
- status download: ok


### Documents

Nenhum (`documents: []`).

### Releases

Nenhum (`releases: []`).

### Customer needs

Nenhum (`customerNeeds: []`).

## Comentários (ordem cronológica por `createdAt`)

`list_comments`: `limit` 250, `orderBy` createdAt, `hasNextPage` false. Total: 7. Ordenados aqui por `createdAt` crescente (a API devolveu o mais novo primeiro).

### Comentário 1 — `6d4c1fd5-8b7b-41da-bec8-282d5faa2035`

- **createdAt:** 2026-09-14T16:28:45.038Z
- **updatedAt:** 2026-09-14T16:28:44.937Z
- **parentId:** null
- **resolvedAt:** null
- **quotedText:** null
- **author:** Maria Eduarda M Camargo (`d540a312-a721-479d-9da4-3b86ed7868ba`)
- **onBehalfOf:** null
- **attachments:** []



#### Corpo integral

## Entregue — relatório item por item, com os números medidos

Tudo abaixo foi **medido no navegador**, não estimado. A sonda é `scripts/medir-cta-referencia-sis253.mjs` (Playwright + sharp, já no repositório) e o relatório cru fica em `docs/medidas/cta-referencia-sis253.json`. Rodada final: saída 0.

### O que fazer · 1 — opt-in só em `/esg`
Prop nova `layoutReferencia` em `ContactCTA.tsx`, **nascendo desligada**, com bifurcação depois de todos os hooks; com ela desligada o módulo novo (`src/components/ContactCTAReferencia.tsx`) nem chega a ser renderizado. Montagem em `src/app/esg/page.tsx`: `<ContactCTA layoutReferencia contatoNoModal />`.

Medido nas três rotas que montam o bloco:

| rota | `.cta-ref` presente | cartão navy | fundo do cartão | cor do título | botão é `<Link>` |
|---|---|---|---|---|---|
| `/solucoes` | não | sim | `linear-gradient(135deg, rgba(0,62,118,.95) 0%, rgba(0,88,155,.9)…` | `rgb(255,255,255)` | sim |
| `/quem-somos` | não | sim | idem | `rgb(255,255,255)` | sim |
| `/blog` | não | sim | idem | `rgb(255,255,255)` | sim |

### O que fazer · 2 — composição da PNG (1440×900)

| elemento | medida |
|---|---|
| cartão de vidro | 826×320 em (162, 290) |
| título | 728×54 em y 403, **49px** (`clamp(2.5rem, 3.4vw, 3.25rem)`) |
| parágrafo | 544×52 em y 459 |
| fio ciano | 700×105 em y 345 — corre **58px acima do título**, nó na ponta esquerda |
| botão pílula | 227×56 em (713, 422), centro y **450** = meia-altura do cartão |
| círculo ciano | 45×45, centro (912, 450) |
| arte (blob) | 446×446 em (921, 227), sangrando à direita |
| órbitas / balões | 2 / 2 |
| teste de acerto no centro do botão | `BUTTON.cta-ref-botao` |

A 390: cartão 350×283, fio 68px acima do título, botão 227×56 alcançável, arte 266×266 — tudo em quadro, e o acerto no centro do botão também é o `BUTTON`.

**Quatro diferenças contra o PNG foram encontradas na conferência visual e fechadas** (a primeira versão passava nos números e ainda não batia com a arte): o botão saiu do rodapé do cartão para a **meia-altura**; o blob ficou **maior (32% → 40%) e mais orgânico**, com aresta ciana em máscara cônica; os balões ganharam **bico**; o título subiu de 36px para 49px.

### O que fazer · 3 — os três movimentos

**Percurso do ponto (entrada no scroll).** Antes de entrar em quadro o ponto está exatamente sobre o nó (distância **0px**) e invisível (`opacity 0`). Depois da entrada está a **698px** do nó, visível, e o erro de pouso sobre o centro do círculo ciano é de **1px** (`dx −1, dy 0`). Um quadro no meio do trajeto foi capturado: ponto em (470, 607) contra o círculo em (912, 705) — `aindaNaoChegou: true`.

**Hover.** Transição `0.42s`. Botão: `matrix(1,0,0,1,0,−28)` em repouso → `matrix(1,0,0,1,6,−28)`, isto é, **+6px** em X (o −28 é a centralização na meia-altura). Brilho do círculo: `none` → `matrix(−1,0,0,−1,0,0)` = **180°**.

**Reduce, pelas DUAS vias** (`reducedMotion` do sistema e `sistran-motion-preference=reduce`) — resultados idênticos: ponto já pousado (**1px**) e visível, transição `1e-05s`, `transform` do botão **igual em repouso e no hover**, avanço **0px**, brilho `transform: none`, título/parágrafo/botão em quadro.

> Uma decisão de implementação que vale registrar: no reduce o botão **não** vai para `transform: none`. Ele carrega a centralização na meia-altura nesse mesmo `transform`, e zerá-lo derrubaria o layout justamente para quem pediu menos movimento. O avanço é uma variável CSS própria, e é ela que o reduce zera.

### O que fazer · 4 — modal
`dialog.contact-dialog` entra no DOM, `open: true`, com `<form>` dentro. São 2 `<dialog>` na rota — o outro é o de preferência de movimento.

### O que fazer · 5 — contraste AA, capturas, lint

Método de `docs/medidas/COMO-MEDIR-CONTRASTE.md` (tinta do `h2` e do `p` do cartão apagada junto, pior pixel, alfa composto, 1440×900, `header.fixed` e avisos do `next dev` escondidos). Reporto o **par**, como o método manda:

| alvo | tinta | piso | raster (pior pixel) | calculada | Δ | passa |
|---|---|---|---|---|---|---|
| título (49px) | `rgb(10,31,68)` | 3,0 | 3,25 | **12,63** | 9,38 | sim |
| parágrafo (16px) | `rgb(60,90,122)` | 4,5 | 2,36 | **5,47** | 3,12 | sim |

A calculada é o veredito; os Δ > 1,0 nos dois são a assinatura de franja de antialiasing que o próprio método prevê.

**Lint:** `npm run lint` → **0 erros, 22 avisos** (os avisos são pré-existentes, em `PillarsCarousel.tsx` e vizinhos, nenhum nos arquivos desta issue).

**Copy-lock:** `npm run test:copy` **falha**, e o relato honesto é este — são três divergências e **só uma é minha**: `Fale com a SISTRAN` foi de 2 para 3 ocorrências **no código-fonte**, porque o rótulo aparece literal também no componente novo. É a mesma frase, palavra por palavra, copiada do botão de origem: nenhuma escrita nova entrou. As outras duas (`Sistran` 12→11, `Sistran University` 2→3) não encostam nesta issue — nenhum arquivo que eu toquei contém essas palavras; vêm de trabalho não comitado que já estava na árvore. **Não atualizei o lock**: mexer nele é decisão de quem é dono do conteúdo.

### O defeito de 16px que a medição pegou

Vale ficar escrito, porque nenhuma leitura de código o teria mostrado. Na primeira versão o ponto pousava **16px à esquerda** do círculo (`dx: −16`). Causa: o cartão é elemento de fluxo, então os 74% da largura dele resolvem contra a **caixa de conteúdo** do `.container-lp` (1116px a 1440, já descontado o `px-8`); o botão é absoluto, e porcentagem de `right` resolve contra a **caixa de padding** (1180px). Duas bases diferentes, 26% da diferença de 64px ≈ 16,6px. Correção: o palco passou a ser um filho sem padding do `container-lp` em vez do próprio container, e as duas contas caíram na mesma base — erro de pouso **16px → 1px**. A conta está comentada no componente, com o `dx: −16` como prova.

### Um ponto para a sua decisão

O mount de `/esg` tinha `haloClaro reativo` (SIS-142). Nesta variante os dois não têm efeito: o que eles decoram (o cartão navy, o halo atrás dele e o grafismo de ponteiro sobre ele) é exatamente a superfície que a referência substitui. Deixei as duas props **comentadas no mount, com o motivo** — não deletadas. Se a intenção for manter alguma reação de ponteiro no cartão de vidro, é uma issue nova.

### Capturas

Estão em `docs/capturas/` (imagem não sobe pelo MCP do Linear — se quiser anexar aqui, é colar):
- `sis253-depois-1440-repouso.png`
- `sis253-depois-1440-hover.png`
- `sis253-depois-1440-percurso.png` (quadro no meio do trajeto)
- `sis253-depois-1440-reduce.png`
- `sis253-depois-390.png`

Vai para **In Review** com `conferir`. Não aplico `conferido` e não fecho sem o seu aceite.

### Comentário 2 — `ab84ea01-1dbe-4214-9201-448f4e361adc`

- **createdAt:** 2026-09-14T18:09:14.092Z
- **updatedAt:** 2026-09-14T18:09:14.054Z
- **parentId:** null
- **resolvedAt:** null
- **quotedText:** null
- **author:** Maria Eduarda M Camargo (`d540a312-a721-479d-9da4-3b86ed7868ba`)
- **onBehalfOf:** null
- **attachments:** []



#### Corpo integral

## 2ª volta — reprovada por fidelidade ao PNG, refeita e medida de novo

A volta anterior foi **reprovada**: «na pagina /esg no Fale com a gente esta assim … deve ficar assim exatamente igual `public/imagensexemplo/falecomagente.png` **a parte de tras do card tambem**». Voltei para In Progress, capturei o estado entregue em 1440 e comparei com a referência antes de mexer em nada. As cinco diferenças abaixo foram as que sobreviveram à comparação — cada uma com o motivo pelo qual o desenho anterior não chegava lá.

### 1. O fundo atrás do cartão — a diferença que a reprovação nomeia

O fundo eram três `radial-gradient` na própria seção, e o comentário do `globals.css` registrava a escolha assim: «as "ondas" são radiais de branco em % — o desenho da referência é luz difusa, não uma curva desenhada». **Comparado ao PNG isso estava errado**: na referência há curvas de verdade — faixas largas atravessando a seção de canto a canto, que aparecem sobretudo *através* do vidro do cartão. Luz radial não produz aresta curva nenhuma, então nenhum ajuste de porcentagem chegaria lá; precisava de caminho.

Entrou `.cta-ref-ondas`: um SVG estático de cinco faixas em traço largo (`preserveAspectRatio="none"`, viewBox 1440×576), `z-index: 0` — acima da luz radial (que ficou, porque a referência tem as duas coisas) e **abaixo** do cartão, que é a ordem do PNG. Sem `transition`, sem `filter`, sem RAF: a camada nova não entra em conta de FPS. `aria-hidden`.

### 2. O cartão lia como retângulo branco chapado

Vidro a 82%→58% de branco sobre um gradiente quase branco não tinha o que revelar. Agora 60%→40%, `blur(22px)`, e o realce interno de aresta (`inset` no mesmo `box-shadow`) que dá espessura ao vidro. **O piso disso é o contraste do texto, e é ele que decide quão longe podia descer** — números no item de aceite abaixo.

### 3. O blob estava apagado — era a diferença mais gritante

A rampa era `#0f6fb8 → #054b86 → #00305c`: três navies. No PNG o blob é **luminoso**, com azul vivo no miolo alto, escurecendo só na saia inferior. Com a rampa antiga a forma lia como pedra escura ao lado de um fundo claríssimo, e nenhuma órbita mais brilhante consertaria isso — o que faltava era o miolo. Agora `#2fa6f2 → #0d78cc → #06508f → #032f60` (quatro paradas, porque com três o salto para o navy era banding visível a 350px de largura), halo ciano dobrado. Os dois extremos de marca (ciano de luz, navy de base) são os mesmos.

### 4. Órbitas e ponto quase não existiam

1px de ciano a 55% sobre um blob escuro é um risco de meio tom; no PNG as órbitas são o segundo elemento mais brilhante da composição, depois do selo. Agora 2px, alfa 95%, halo por fora e por dentro — os três juntos, porque nenhum resolve sozinho (alfa sem halo lê como aresta serrada; halo sem espessura dissolve a linha). O ponto da órbita foi de 9px para 16px, com miolo branco, e saiu de dentro do corpo escuro (`right: 13%`) para a borda direita (`right: 5%`), onde a referência o põe.

### 5. Balões: pastilhas viraram falas de vidro

Corpo 22%→34%, borda 42%→62%, pontos 9px→11px, bico maior. Dois consertos de leitura: o balão de cima estava em `left: 4%`, **fora** do blob, flutuando no fundo claro — foi para cima do ombro esquerdo do selo; e o bico do balão de baixo apontava para **cima**, de volta para o selo, invertendo a leitura — os dois agora apontam para baixo, como no PNG.

Mais: o selo foi de 42% para 48% da largura do blob, e a sombra preta (`rgb(0 0 0 / 35%)`) que sujava o branco contra um blob agora claro deu lugar a halo ciano — a regra de glow do projeto. O rótulo do botão foi de 14px para 15px, que é o corpo que ele tem na referência.

---

## Itens de aceite, um por um

**Bate com `falecomagente.png`, incluindo o fundo atrás do cartão** — cumprido. `docs/capturas/sis253-esg-cta-1440-atual.png` (o quadro reprovado) contra `sis253-esg-cta-1440-depois.png`, ambos 1440×576 na mesma seção. As cinco diferenças acima estão fechadas. **Uma divergência assumida, não esquecida:** no PNG o ponto do fio termina num nó luminoso **acima** do botão; aqui ele pousa no centro do círculo ciano. É a geometria medida da 1ª volta (`FIO_CAMINHO` ancorado por `bottom: 50%`/`right`, erro de pouso ~1px) e mudá-la refaria a âncora inteira por ~20px de posição de um ponto. Se você quiser o pouso da referência, é uma linha de ajuste — diga e eu faço.

**O ponto percorre a linha até o botão na entrada** — cumprido e medido depois das mudanças, que é o que importava conferir: baixar a opacidade do vidro e engordar o botão não desalinhou nada. Pouso medido **dx −1px, dy 0px** do centro do círculo (`docs/medidas/cta-referencia-sis253-depois.json`). GSAP MotionPath + ScrollTrigger `start: 'top 78%', once: true`, HTML servindo o ponto já pousado.

**Botão avança no hover e o círculo gira** — cumprido, pelos `transform` computados: botão `matrix(1,0,0,1,6,-28)` (6px em X somados à centralização de meia-altura, que sobrevive porque o avanço é variável e não `transform` inteiro) e brilho `matrix(-1,0,0,-1,0,0)` = 180°. Captura `sis253-esg-cta-1440-hover.png`.

**Movimento reduzido entrega tudo estático** — cumprido nas **duas** vias, medido em cada uma:
- via `@media (prefers-reduced-motion: reduce)`: botão `matrix(1,0,0,1,0,-28)` — avanço zerado, centralização intacta.
- via `html[data-motion='reduce']`: botão `matrix(1,0,0,1,0,-28)` e brilho `transform: none`.

Captura `sis253-esg-cta-1440-reduce.png`. O ponto do fio não aparece nas regras porque o `useEffect` lê a preferência de forma síncrona e não chega a tocar nele — fica pousado no fim do fio, que é o estado que o HTML serve.

**Contraste do texto escuro sobre o vidro** — medido no método p5 tinta × p95 fundo, `deviceScaleFactor: 2`, 1440×900, idêntico nas duas vias de movimento:

| alvo | razão | tinta sobre fundo |
|---|---|---|
| título «Fale com a Gente!» | **16,25:1** | `rgb(10,31,68)` sobre branco |
| parágrafo | **6,68:1** | `rgb(60,90,122)` sobre `rgb(242,248,253)` |
| rótulo do botão | **5,75:1** | `rgb(6,106,170)` sobre branco |
| disco ciano do botão | **2,41:1** | `rgb(27,179,236)` sobre branco |

Os três primeiros passam AA de corpo de texto (4,5:1) com folga — o vidro mais translúcido não custou contraste. **O 2,41 do disco é deliberado e está escrito na folha:** a seta ficou branca como na referência (era navy `#04263f`), e branco sobre `#17b2ec` dá 2,41:1, abaixo do 3:1 de objeto gráfico. Fica assim porque a seta **não carrega informação** — o nome acessível do botão é o texto «Fale com a SISTRAN», ao lado dela com 5,75:1; um ícone que só repete o rótulo não é gráfico necessário para entender o conteúdo (WCAG 1.4.11). O comentário registra a condição de reversão: se algum dia a seta ficar sozinha num botão só de ícone, o navy tem de voltar. Pelo mesmo motivo o brilho cônico caiu de 70% para 34% de branco — a 70% ele **apagava** a seta branca durante o giro.

**Outras rotas intactas, modal de `/esg` funcionando** — conferido no código: `layoutReferencia` só é passado em `src/app/esg/page.tsx:884`, e todo seletor novo começa em `.cta-ref-` (inclusive `.cta-ref-ondas`, que é filho do `ContactCTAReferencia`). As outras sete telas montam o cartão navy do `ContactCTA` e não têm como alcançar nada disto. `contatoNoModal` continua na chamada, então o botão segue sendo `<button onClick={onContato}>`.

**Capturas e lint** — `npm run lint`: **22 problemas, 0 erros** (22 avisos, o mesmo número da baseline do repositório). Capturas em `docs/capturas/`: `sis253-esg-cta-1440-atual` (reprovado), `-depois`, `-hover`, `-reduce` e `sis253-esg-cta-390`.

**Mobile** — 390×844 conferido: fluxo vertical (cartão → botão → arte), `scrollWidth` = 390, nada recortado nem sobreposto. Não é a timeline do desktop comprimida.

## Arquivos alterados

- `src/components/ContactCTAReferencia.tsx` — camada `.cta-ref-ondas` (SVG estático, `aria-hidden`). Nada mais: texto, props, GSAP, ordem do DOM e contrato de movimento intactos.
- `src/app/globals.css` — bloco SIS-253: ondas, vidro do cartão, fio com halo, disco/seta/brilho do botão, blob, órbitas, ponto, selo, balões.
- `scripts/medir-cta-sis253.mjs` — sonda dos portões (contraste, pouso, `transform` de hover nas duas vias), para os números serem refeitos e não relidos.
- `docs/medidas/cta-referencia-sis253-depois.json` — saída da sonda.

## Pendência real

Uma só, e é de decisão sua, não de execução: o pouso do ponto (nó luminoso acima do botão, como no PNG, contra o pouso no círculo que a 1ª volta mediu). Está descrito no primeiro item de aceite acima.

### Comentário 3 — `c3251eb5-09c6-4b56-8e30-63851ce981e9`

- **createdAt:** 2026-09-14T18:41:50.622Z
- **updatedAt:** 2026-09-14T18:41:50.574Z
- **parentId:** null
- **resolvedAt:** null
- **quotedText:** null
- **author:** Maria Eduarda M Camargo (`d540a312-a721-479d-9da4-3b86ed7868ba`)
- **onBehalfOf:** null
- **attachments:** []



#### Corpo integral

## 3ª volta — aplicado `docs/orientacoes-componente-fale-com-a-gente.md`

A orientação foi tratada como especificação: cada seção dela virou uma mudança, e cada item numérico do §12 virou um portão medido. Os números abaixo saem de `docs/medidas/cta-referencia-sis253-depois.json` (`node scripts/medir-cta-sis253.mjs`, 1440×900, `deviceScaleFactor: 2`).

### §3 e §8 — a forma azul deixou de ser `border-radius`

O doc é explícito: «não utilizar `border-radius` aleatório para construir a forma principal — isso tende a produzir o volume irregular visto no componente atual». A crítica está certa contra o que havia. `border-radius: 62% 38% 46% 54% / 42% 58% 42% 58%` é um losango de quatro arcos elípticos com tangente vertical ou horizontal nos quatro pontos médios das arestas: **não existe combinação desses oito números que produza curvatura variável** — só círculo (valores iguais) ou losango de cantos moles (valores desiguais). Eram exatamente os dois defeitos do §2 («quase circular», «silhueta irregular»).

Agora é um `<path>` Bézier em SVG, com o `d`, o `viewBox 760×620` e o gradiente de três paradas do §5, sem alteração. O que morreu na troca, cada um com substituto nomeado no comentário do componente: o `border-radius`, o `radial-gradient` de quatro paradas terminando em `#032f60`, o `box-shadow` de três camadas e o `::after` de máscara cônica.

| Item do §12 | Medido | Faixa do doc |
| --- | --- | --- |
| 1 · mais larga do que alta | **1,121** (larg./alt. do `<path>`) | > 1 |
| 3 · base não escura demais | `#064C98` (era `#032f60`) | azul médio |
| 4 · painel sobrepõe a forma | **89px** | 80–150px |
| 6 · símbolo centrado *visualmente* | `left: 57%` · **38,0%** da arte | ~38% |
| 7 · órbitas sem “X” evidente | abertura **28°** (−8°/+20°, era 40°) | — |
| §8 · órbitas | **94,0%** da arte | 90–96% |
| §8 · balões | **18,0%** e **18,0%** | ~18% |
| 8 · balões não cortados | `true` (1440 e 390) | — |
| 10 · sem rolagem horizontal | `scrollWidth` **1440** / **390** | — |
| 12 · símbolo com transparência real | `sistran-logo.png`, alfa 0–255 | — |

### §4 — a ordem das camadas, e a inversão que ela obriga

`z-index` computados: ondas `0` · **arte `1`** · forma `2` · órbitas `3` · selo `4` · balões `5` · cartão `2` (acima da arte) · botão `4`.

A arte estava em `z-index: 3`, **acima** do cartão, e a nota antiga justificava com «o blob entra por cima do canto chanfrado». O §2/§4/§7 pedem o contrário («o painel esquerdo sobrepõe parcialmente o fundo azul»), e o doc está certo: o que o chanfro faz é *abrir* o canto para a forma aparecer, e para isso ela não precisa estar na frente. Com o cartão em cima, o recorte mostra a forma limpa no chanfro e o vidro a mostra difusa no resto da sobreposição. Com a arte na frente, o azul cobria a aresta do cartão e o painel deixava de ser primeiro plano.

A largura da arte foi de 40% para **42%** por causa do número: com 40% a sobreposição dava 71px, abaixo da faixa de 80–150px do §8. Cada ponto percentual vale ~11,8px a 1440.

### §5 e §6 — símbolo, órbitas, balões

- **Símbolo**: 48% → **38%**, `left: 57%`. O §5 pede `/images/sistran-symbol.svg`; **o arquivo não existe no projeto** e eu não inventei o caminho — servir 404 seria pior. O que está lá já cumpre a exigência real do doc («não utilizar uma imagem com fundo branco incorporado»): `sistran-logo.png` é 442×457 **com canal alfa de verdade** — medido, o alfa vai de 0 a 255, isto é, há pixels totalmente transparentes.
- **Órbitas**: mesma largura nas duas (94%), altura 50%, `left: 55%`. A discrição da segunda passou de `border-color: 60%` para `opacity: 0.55` (§6): com larguras iguais, baixar só a cor deixava a linha nítida e cinzenta; `opacity` apaga borda **e** halo, que é o que dá o “atrás”.
- **Balões**: o tamanho passou a ser **medido** e não somado a partir do `padding`. Antes era `padding` + pontos de 11px, isto é, px fixos — a mesma placa valia ~11% da arte a 1440 e ~30% dela a 390. Agora `width: 18%` + `aspect-ratio: 138/78`, e os pontos em `14%` da placa (≈11px a 1440, o diâmetro que já estava certo lá). Vidro azulado do §6, borda de 2px.
- `.cta-ref-balao-b` saiu de `right: -6%` para `right: 1%`: a arte já sangra `-8%` para fora do palco, os dois desvios somavam e o balão passava da borda da seção, que tem `overflow: hidden`. Era o item 8 do checklist a um pixel de falhar.
- `.cta-ref-arte` recebeu `overflow: visible` declarado (§4), porque as órbitas passam de 100% da largura.

### §10 — o movimento lento, com as DUAS vias

`cta-ref-flutua` 6s no selo e `cta-ref-orbita-pulsa` 4,5s nas órbitas, com os tempos e a amplitude do doc (10px).

| | movimento normal | `@media reduce` | `html[data-motion='reduce']` |
| --- | --- | --- | --- |
| selo | `cta-ref-flutua` | `none` | `none` |
| órbitas | `cta-ref-orbita-pulsa` | `none` | `none` |
| selo, `transform` parado | — | — | `matrix(1,0,0,1,-89,-92)` (centrado) |

O teto da pulsação é **por órbita** (`--cta-ref-orbita-op`), e não o par 0,55 → 0,9 do doc: a órbita “de trás” está a 55%, e a pulsação do doc a levaria acima do repouso da da frente, invertendo qual está na frente no meio de cada ciclo.

Em CSS e não em GSAP, com a skill de movimento aberta: são loops decorativos independentes de scroll e de ponteiro, então o GSAP só acrescentaria um RAF permanente e um caminho pelo qual o JS decidiria se o movimento existe. Só `transform` e `opacity`. Quem flutua é o `<span>` envelope, não a `<img>` — ela tem `drop-shadow`, e animar transform num nó com filtro recomputa a sombra a cada quadro.

### Os portões da 2ª volta, reconferidos

| | valor |
| --- | --- |
| título | **16,25:1** |
| parágrafo | **6,68:1** |
| rótulo do botão (glifos) | **16,25:1** |
| disco ciano × seta branca | **2,39:1** (deliberado, ver abaixo) |
| pouso do ponto no círculo | `dx −1px`, `dy 0` |
| hover | botão `matrix(1,0,0,1,6,−28)` · brilho 180° |
| reduce (as duas vias) | botão `matrix(1,0,0,1,0,−28)` · brilho `none` |
| `npm run lint` | **22 problems / 0 errors** (baseline do repo) |
| mobile 390 | arte `position: relative` (volta ao fluxo, §9), 266×217, nada cortado |

### ⚠️ Uma correção de método, e ela muda um número que já reportei

O **5,75:1 do rótulo do botão que reportei na 2ª volta estava medindo a coisa errada**, e o 3,49:1 que a sonda devolveu agora também estaria. Os dois recortavam a **pílula inteira** (237×56px), da qual o texto ocupa ~12% da área e o disco ciano outros ~14%. Num recorte assim o percentil 5 não cai necessariamente na tinta — ele cai onde estiverem os 5% mais escuros do conjunto, e basta a proporção entre pílula, glifos e disco mudar para o número andar sem que cor nenhuma tenha mudado. Foi o que aconteceu entre as duas rodadas com `color: #0a1f44` e `background: #fff` **intactos**.

A sonda agora mede também `rotuloGlifos`, recortando só a caixa do nó de texto via `Range`: **16,25:1**. Esse é o número do rótulo; os 5,75 e os 3,49 descrevem a moldura, não a letra. Os dois ficam no JSON.

Pelo mesmo motivo a proporção das órbitas foi remedida: `getBoundingClientRect` de uma elipse **rotacionada** devolve a caixa alinhada aos eixos, sempre maior — deu 98,8% para uma elipse declarada em 94%, e “corrigir” isso teria tirado da faixa do §8 um valor que já estava dentro dela. Vale `offsetWidth`. As duas leituras ficam no JSON (`orbitaPctArte`, `orbitaBboxPctArte`).

### Pendências reais

1. **O disco ciano continua em 2,39:1 com a seta branca.** É a mesma decisão da 2ª volta, com o mesmo registro na regra e a mesma condição de reversão escrita: a seta não carrega informação (o nome acessível é o rótulo visível, a 16,25:1), então WCAG 1.4.11 não se aplica a ela — se algum dia virar ícone sozinho, o navy `#04263f` volta. Segue sendo decisão sua, não fato consumado.
2. **A silhueta do §5 não é tão larga quanto a do PNG.** O `<path>` do doc dá 1,121 de razão; na referência a forma é visivelmente mais achatada. Cumpri o doc à letra porque ele é a especificação mais recente — se o alvo é o PNG, o `d` precisa ser outro, e aí é o doc que muda.
3. Aviso do Next no console: `sistran-logo.png` detectada como LCP, sugerindo `loading="eager"`. A imagem não está acima da dobra (o bloco fecha a página) e nada nos atributos dela foi tocado — só a largura renderizada. Não mexi.

### Arquivos

- `src/components/ContactCTAReferencia.tsx` — forma azul em `<path>` com gradiente, filtro de sombra + halo ciano e aresta traçada; ordem das camadas comentada.
- `src/app/globals.css` — bloco `.cta-ref-*`: caixa da forma, proporção 760/620, `overflow: visible`, selo, órbitas, balões, `z-index` do §4, os dois `@keyframes` do §10 e as duas vias de movimento reduzido.
- `scripts/medir-cta-sis253.mjs` — portões do §12, medição por glifos do rótulo, `offsetWidth` das órbitas, via `data-motion` e passe de 390.
- `docs/medidas/cta-referencia-sis253-depois.json` · `docs/capturas/sis253-esg-cta-1440-{depois,hover,reduce}.png`, `sis253-esg-cta-390.png`.

### Comentário 4 — `51f79599-df5f-45e1-b7f0-3f991f85920f`

- **createdAt:** 2026-09-14T19:01:48.943Z
- **updatedAt:** 2026-09-14T19:01:48.922Z
- **parentId:** null
- **resolvedAt:** null
- **quotedText:** null
- **author:** Maria Eduarda M Camargo (`d540a312-a721-479d-9da4-3b86ed7868ba`)
- **onBehalfOf:** null
- **attachments:** []



#### Corpo integral

## 3ª volta — escala e posicionamento (override aplicado)

A reprovação era: «todo o conjunto da direita está pequeno e deslocado para cima/esquerda; o painel está estreito e o botão está no centro do painel, quando deveria ficar na junção com o elemento azul». O desenho do SVG **não foi tocado** nesta volta, como pedido.

**A causa raiz, medida.** Os números do override são contados a partir da borda da **janela**; o palco é filho do `.container-lp`, que é centrado e tem `max-width: 1180px` — a 1440 ele começa em x=162. Escrever `left: 52.3vw` direto colocaria o botão em ~915 em vez de 753, e o mesmo erro de 162px encolhia painel e arte. Como o container é centrado, dentro dele a borda esquerda da janela está em `50% − 50vw`: a conversão exata é `calc(X − 50vw + 50%)`, e ela está escrita por extenso no cabeçalho do bloco `@media (min-width: 1024px)`. Os seis números do override viraram variáveis no `.cta-ref-palco` porque o pouso do ponto do fio é a soma de quatro deles — soltos, mexer no botão desalinharia o pouso em silêncio (foi o que produziu `dx: -16` na 1ª volta).

### Os quatro ajustes essenciais

| pedido | aplicado | medido a 1440 |
|---|---|---|
| `.contactPanel` 57vw → **67vw** | `--cta-ref-cartao-w: min(67vw, 1380px)` | painel 51,8 → **1016,6** (964,8px de largura) |
| `.contactVisual` **37vw** | `--cta-ref-arte-w: clamp(520px, 37vw, 760px)` | arte 864 → 1396,8 (532,8px) — cresceram azul, logo, órbitas e balões com a caixa |
| `.contactCta` **`left: 52.3vw` / `top: 56%`** | `left: calc(52.3vw − 50vw + 50%)`, `right: auto`, `top: 56%` | botão **753 → 1048** |
| título **`font-weight: 700`** | `font-weight: 700` no seletor de duas classes | + `clamp(3.25rem, 3.9vw, 5rem)`, `line-height: .98`, `letter-spacing: −.035em` |

### A relação pedida

`botaoSobreEncontro` (sonda): `botaoEsq 753 · cartaoDir 1017 · botaoDir 1048 · arteEsq 864`.
O botão **atravessa** a borda direita do painel (1017 está entre 753 e 1048) e entra sobre a forma azul (864 < 1048). Deixou de haver espaço vazio entre botão e azul: **painel → botão sobreposto à borda → elemento azul parcialmente atrás**.

### Portões medidos — `docs/medidas/cta-referencia-sis253-depois.json`

- **Pouso do ponto do fio no círculo ciano:** `dx: -2, dy: 0` px. É o número mais em risco ao mover o botão; as três âncoras de `.cta-ref-fio` (`left`, `right`, `bottom: 44%`) foram refeitas juntas para acompanhar `top: 56%`.
- **Contraste** (p5 tinta × p95 fundo, `deviceScaleFactor: 2`, 1440×900): título `#06275f` sobre o vidro novo de 64% = **13,64:1**; parágrafo **6,68:1**; rótulo do botão (recorte dos glifos por `Range`) **16,25:1**. Disco ciano **2,41:1**, deliberado e com a condição de reversão escrita na regra `.cta-ref-botao-circulo`.
- **Movimento reduzido, nas DUAS vias:** `@media` → `selo: none`, `orbita: none`; `html[data-motion='reduce']` → idem. Hover com movimento reduzido: `matrix(1,0,0,1,0,-30.95)` — só a centralização, **sem** o avanço de 6px. Hover normal: `matrix(1,0,0,1,6,-30.95)` e o brilho a 180°.
- **Checklist §12:** forma 1,121 mais larga que alta · selo 38% da arte · órbitas 97,9% · balões 19%/19% · abertura das órbitas 28° · balões dentro da seção a 1440 **e** a 390 · `scrollWidth` 1440 e 390 (sem rolagem horizontal) · símbolo com alfa real · camadas `{ondas 0, arte 1, blob 2, cartão 2, órbita 3, selo 4, botão 4, balão 5}`.
- **Mobile 390:** arte volta ao fluxo (`position: relative`, 266×217), balões dentro, sem rolagem horizontal.
- **`npm run lint`: 22 problems / 0 errors** — a linha de base do repositório, sem problema novo.

### Diferenças declaradas (não são esquecimento)

1. **O chanfro do canto superior direito saiu**, comentado com o motivo. Duas razões geométricas: o override declara `border-radius` com raios de até 210px à direita, e `clip-path` e raio disputam o mesmo canto (o recorte vence, o raio pedido nunca apareceria); e o `clip-path` recorta descendentes — o fio agora sangra ~4px além da borda do cartão para alcançar o centro do círculo, e a ponta dele (com o pouso do ponto) seria cortada.
2. **Sobreposição painel↔forma = 153px**, 3px acima da faixa de 80–150px do §8 do doc. Os dois lados dessa conta (67vw e 37vw) saem do mesmo override; o override é a ordem mais recente.
3. **Órbitas a 97,9%**, fora da faixa de 90–96% do §8 — valor explícito do override (`98%`), pedido porque a forma cresceu e o anel não acompanhava.
4. **Balões em porcentagem (19%) e não nos `clamp` em px do override.** `clamp(100px, 7vw, 144px)` = 100,8px a 1440 = 18,9% da arte de 532,8px, ou seja, o mesmo tamanho — mas px dentro de uma caixa dimensionada em `vw` é a armadilha de proporção já corrigida na volta anterior (o balão dava ~11% da arte a 1440 e ~30% a 360).
5. **`height` do painel virou `min-height`** (mesmo `clamp(340px, 21vw, 430px)`): título e parágrafo são texto real vindo por prop, e altura fixa faria uma frase mais longa transbordar o vidro.
6. **Não aplicados, por não haver alvo:** o `translateY` de `.contactContent` (não existe esse nó; o `padding-top: 7rem` já põe o conteúdo abaixo do fio) e o `min-height` da seção (a altura medida já o excede). O `top: 34%` do conector também não: aqui o fio é ancorado por baixo, e é o `bottom` que faz o ponto pousar.

Capturas: `docs/capturas/sis253-esg-cta-1440-depois.png`, `-hover.png`, `-reduce.png`, `sis253-esg-cta-390.png`.

### Comentário 5 — `be7aab35-308d-4abc-91d6-2a156cda5c86`

- **createdAt:** 2026-09-14T19:21:15.342Z
- **updatedAt:** 2026-09-14T19:21:15.313Z
- **parentId:** null
- **resolvedAt:** null
- **quotedText:** null
- **author:** Maria Eduarda M Camargo (`d540a312-a721-479d-9da4-3b86ed7868ba`)
- **onBehalfOf:** null
- **attachments:** []



#### Corpo integral

## 4ª volta — «diminua o tamanho de todos os componentes esta muito grande tudo»

O pedido é sobre a composição que a 3ª volta produziu a pedido explícito (painel 67vw, visual 37vw, botão em `left: 52.3vw`). O que mudou agora é **só a escala**: a relação exigida — «painel → botão sobreposto à borda → elemento azul parcialmente atrás» — continua medida e cumprida. Nenhum arquivo de componente foi tocado; tudo em `globals.css`, no bloco `@media (min-width: 1024px)`.

### A conta que obrigou as margens a CRESCEREM enquanto tudo encolheu

É a única parte contraintuitiva, e sem ela a redução destruiria o desenho. A sobreposição é a sobra de tinta sobre a janela:

```
sobreposição = margem + painel + margem-da-arte + arte − 100vw
```

Painel e arte estão ancorados em bordas **opostas** da janela. Encolher os dois mantendo as margens antigas subtrai duas vezes da mesma soma: a 1440, `58vw + 32vw` com as margens velhas dava **−27px** — uma fresta entre o painel e o azul, que é exatamente o defeito «painel → botão → espaço vazio → elemento azul» que a volta passada corrigiu. Então a composição inteira se afastou das duas bordas: ela deixou de sangrar na janela e passou a ser um bloco menor e mais centrado.

### O que mudou, número a número

| variável | 3ª volta | agora | a 1440 |
|---|---|---|---|
| `--cta-ref-margem` | `clamp(32px, 3.6vw, 72px)` | `clamp(40px, 9vw, 170px)` | 129,6 |
| `--cta-ref-cartao-w` | `min(67vw, 1380px)` | `min(58vw, 1180px)` | 835,2 |
| `--cta-ref-arte-w` | `clamp(520px, 37vw, 760px)` | `clamp(420px, 32vw, 640px)` | 460,8 |
| `--cta-ref-arte-margem` | `clamp(18px, 3vw, 64px)` | `clamp(24px, 7vw, 130px)` | 100,8 |
| `--cta-ref-botao-esq` | `52.3vw` | `55vw` | 792 |
| `--cta-ref-botao-w` | `clamp(290px, 20.5vw, 420px)` | `clamp(250px, 18vw, 350px)` | 259,2 |
| `--cta-ref-pad` | `3rem` | `2.5rem` | 40 |
| `min-height` do painel | `clamp(340px, 21vw, 430px)` | `clamp(290px, 17.5vw, 360px)` | 290 |
| `padding-top` do painel | `7rem` | `5.5rem` | 88 |
| raios do painel | `56/120px` (`4.5vw`/`10vw`) | `44/96px` (`3.6vw`/`8vw`) | — |
| altura do botão | `clamp(58px, 4.3vw, 88px)` | `clamp(52px, 3.7vw, 72px)` | 53,3 |
| **título** | `clamp(3.25rem, 3.9vw, 5rem)` | `clamp(2.625rem, 3.1vw, 3.875rem)` | 44,6 |

**Órbitas, selo e balões não aparecem na tabela e encolheram junto** — são % da arte (98%×54%, 38%, 19%), então seguem `--cta-ref-arte-w` sozinhos. É a razão de eles estarem em % e não em px, registrada no código.

O nó vazado do fio (`clamp(54px, 7.5vw, 155px)` → `clamp(70px, 11vw, 210px)`) foi o único número que **não** pôde encolher: a margem do painel cresceu, e um nó a menos de `--cta-ref-margem` da janela cairia fora do vidro, à esquerda dele. Agora ele fica ~29px dentro da borda esquerda do painel a 1440, ~20px a 1024 e ~40px a 1920.

O título era o componente mais desproporcional da captura da reprovação: a `3.9vw` ele dava **75px a 1920**. O teto caiu para 62px e o piso para 42px — continua muito acima de 24px, então o piso AA aplicável segue sendo 3,0:1 e o aceite não muda.

### Portões medidos de novo (`docs/medidas/cta-referencia-sis253-depois.json`)

* **A relação pedida, a 1440**: painel 129,6 → **965**; arte a partir de **878**; botão de **792 a 1051**. O botão atravessa a borda direita do painel (965) e entra na forma azul (878). Sobreposição painel↔forma = **86px**, agora **dentro** da faixa 80–150 do §8 (na volta passada eram 153, 3px fora). A 1920 dá 108px; a 1024 dá 154px, e esses 154 vêm do piso de 420px da arte, que existe para o SVG não virar miniatura.
* **Pouso do ponto do fio no círculo do botão**: `dx: -2`, `dy: 0` — as três âncoras do fio são fórmulas sobre as mesmas variáveis, então a geometria acompanhou a escala sem retoque.
* **Contraste** (p5 tinta × p95 fundo, `deviceScaleFactor: 2`, 1440): título **13,64:1**, parágrafo **6,68:1**, rótulo do botão pelos glifos **16,25:1**, disco ciano **2,41:1** (deliberado, com a condição de reversão escrita no CSS).
* **Movimento reduzido nas duas vias**: `@media` e `html[data-motion='reduce']` → `animation-name: none` no selo e nas órbitas; no hover reduzido o botão não avança (`matrix(1,0,0,1,0,…)` contra `…,6,…` em movimento normal).
* **Checklist do §12**: forma mais larga que alta (1,121), selo 38% da arte, órbitas 98% (declarado, fora da faixa 90–96 do doc — já divulgado na volta passada), balões 19% e **dentro** da seção, abertura das órbitas 28°, `scrollWidth: 1440` (sem rolagem horizontal), símbolo com alfa real.
* **Mobile 390**: arte volta ao fluxo (`position: relative`), 266×217, `scrollWidth: 390`, balões dentro.

### Portão de lint

`npm run lint` → **23 problems (0 errors, 23 warnings)**. Nenhum dos 23 está em arquivo tocado por esta volta (a lista inteira é `react-hooks/static-components` e afins em componentes de carrossel/diálogo, mais dois `postcss.config.mjs`).

### Capturas

`docs/capturas/sis253-esg-cta-1440-depois.png`, `…-1920-depois.png`, `…-1440-hover.png`, `…-1440-reduce.png`, `…-390.png`.

### Comentário 6 — `314d275a-a56f-4521-817b-83f19069cdff`

- **createdAt:** 2026-09-14T19:51:39.027Z
- **updatedAt:** 2026-09-14T19:51:39.003Z
- **parentId:** null
- **resolvedAt:** null
- **quotedText:** null
- **author:** Maria Eduarda M Camargo (`d540a312-a721-479d-9da4-3b86ed7868ba`)
- **onBehalfOf:** null
- **attachments:** []



#### Corpo integral

## 5ª volta — os quatro pedidos, item por item

Pedido literal: «coloque essa linha mais para baixo e deixe passando uma bolinha dinamicamente nessa linha diminua todos os componentes e deixe essa parte nao fique trasparente e deixe o fundo com o mesmo azul que fica no card quando da o hover».

### (a) «coloque essa linha mais para baixo»

`FIO_ALTURA` 150 → **105** em `ContactCTAReferencia.tsx`, e `FIO_CAMINHO` de `M 13 9 H 855 L 1000 150` para `M 13 9 H 904 L 1000 105`.

Por que a razão e não uma coordenada: o invólucro do fio é ancorado no **centro do disco ciano do botão** e cresce para cima em `FIO_ALTURA/1000 × largura do invólucro`. A altura do trecho horizontal é essa razão — baixá-la baixa a linha, e o **pouso não se move**, porque o ponto de chegada é o canto inferior direito do `viewBox`, que sempre cai no centro do círculo. O `H 855 → H 904` acompanha para a diagonal continuar a 45° (96 de avanço para 96 de queda); o SVG escala uniformemente, então o ângulo na tela é o do `viewBox`.

Medido, distância do trecho horizontal ao topo do painel × topo do título:

| largura | fio do topo | título do topo | folga |
|---|---|---|---|
| 1024 | 94 | 114 | 20 |
| 1280 | 85 | 114 | 29 |
| 1440 | 88 | 114 | 26 |
| 1920 | 80 | 114 | 34 |

Antes eram ~32px a 1440 — a linha colava no topo do vidro. Pouso do último vértice do `<path>` no centro do círculo: **dx −2, dy 2** em movimento normal e reduzido.

⚠️ Um defeito estrutural apareceu no caminho e foi corrigido na raiz, não com padding: com o botão em `top: 56%` e o fio em `bottom: 44%`, a altura da linha era `0,56·H − 0,105·W` — dependia da **altura do painel**, que depende de quantas linhas o parágrafo quebra, que depende da largura da janela. Subir `padding-top` alimenta o laço (faz H crescer). A linha de centro do botão passou a ser em **pixels** (`--cta-ref-botao-y-topo: clamp(150px, 11vw, 175px)`) e o `bottom` do fio é o complemento dela, o que tira H da conta.

### (b) «deixe passando uma bolinha dinamicamente nessa linha»

A timeline deixou de ser `once` e virou laço: `repeat: -1`, `repeatDelay: 0.8`, com `toggleActions: 'play pause resume pause'` em vez de `once` — sem isso o RAF do GSAP fica acordado na página inteira depois que a seção sai da tela. O `opacity` de entrada e saída passou para dentro do ciclo, senão o ponto salta de volta ao nó vazado a cada volta.

Prova (12 amostras a 300ms da posição x do ponto):
- normal: `994, 994, 244, 265, 388, 570, 799, 934, 985, 994, 994, 994` → **8 posições distintas**, `reiniciou: true`;
- movimento reduzido: 12 amostras iguais em x 994, `parado: true` — o ponto fica estacionado no fim do fio, que é o estado estático aceito.

Movimento reduzido não precisou de regra nova: `prefersReducedMotion()` é lido **sincronamente antes de qualquer `set`**, então nada é criado.

### (c) «diminua todos os componentes»

Só escala, em `globals.css` (valores da 4ª volta ficam comentados acima de cada um):

| variável | antes | agora |
|---|---|---|
| `--cta-ref-cartao-w` | `min(58vw, 1180px)` | `min(52vw, 1060px)` |
| `--cta-ref-arte-w` | `clamp(420px, 32vw, 660px)` | `clamp(380px, 29vw, 600px)` |
| `--cta-ref-botao-w` | `clamp(225px, 18vw, 360px)` | `clamp(240px, 16vw, 320px)` |
| `--cta-ref-margem` | `clamp(28px, 11vw, 240px)` | `clamp(36px, 14.5vw, 300px)` |
| `--cta-ref-arte-margem` | `clamp(16px, 8vw, 180px)` | `clamp(20px, 10.75vw, 230px)` |
| título | `clamp(2.625rem, 3.1vw, 3.875rem)` | `clamp(2.25rem, 2.7vw, 3.25rem)` |
| `min-height` do painel | `clamp(290px, 17.5vw, 360px)` | `clamp(258px, 15.6vw, 320px)` |

As margens **cresceram** porque painel e arte são ancorados em bordas opostas da janela: `sobreposição = margem + painel + margem-da-arte + arte − 100vw`, então encolher os dois com as margens antigas subtrai duas vezes do mesmo total. Sobreposição medida: **147 / 89 / 90 / 120** px a 1024/1280/1440/1920 — toda dentro da faixa de 80–150px do §8. Órbitas, selo e balões são % da arte (97,8% / 38% / 19%), então encolheram sozinhos. `scrollWidth` = largura da janela em todas.

O botão continua cobrindo o encontro dos dois blocos: a 1440 vai de **792 a 1032**, borda do painel em **958**, arte começa em **868**.

Dois defeitos que a redução expôs a 1024 e foram corrigidos: o rótulo quebrava em duas linhas (piso da largura 225 → **240px**, calculado do conteúdo da pílula ≈226px, mais `white-space: nowrap`), e o parágrafo corria por baixo da pílula (`max-width: min(34rem, calc(botao-esq − margem − pad − 1.5rem))`, geométrico em vez do 34rem fixo).

### (d) «nao fique trasparente … o mesmo azul que fica no card quando da o hover»

`.cta-ref-cartao` recebeu as camadas do hover dos cartões de `/esg` sobre uma base **opaca**, e `backdrop-filter` saiu. `background` é atalho: declarar só a camada nova apagaria a base e o painel voltaria a ser transparente, então as três viajam juntas.

Provado pelo estilo computado: `backdropFilter: none` e **última camada do `background` opaca** (`linear-gradient(135deg, rgb(242,247,252), rgb(228,237,247))`). Aqui não serve a prova por diferença de quadros que o cartão da Carreira usou — atrás deste painel não há vídeo, só as faixas estáticas de `.cta-ref-ondas`. E contar alfas em todas as camadas seria contar errado: as duas de cima são translúcidas de propósito, é o banho ciano.

Contrastes sobre a superfície nova (p5 tinta × p95 fundo, `deviceScaleFactor: 2`, glifos recortados por `Range`), a 1440: título **14,35:1**, parágrafo **4,85:1**, rótulo do botão **16,25:1**. Disco ciano 2,41:1 — deliberado, com a condição de reversão escrita na regra.

O fio ficou **ciano sobre ciano** quando o painel virou azul, e isso anulava o pedido (b): a bolinha era um filete de marca-d'água. Aplicado o precedente que já existe no código — `.esg-superficie:hover` troca a borda para azul primário por exatamente este motivo. Núcleo do traço `rgb(0 121 203 / 90%)`, nó e ponto `#0079cb`, halo ciano mantido (a regra de brilho da marca é ciano, nunca branco frio).

### Perdas declaradas

1. As faixas curvas de `.cta-ref-ondas` **deixaram de aparecer através do painel**. Isso revoga a decisão de translucidez da 2ª volta — os dois pedidos são incompatíveis nessa camada e o mais recente vence. Registrado com ⚠️ na regra, e a nota antiga «Vidro: translúcido…» ficou marcada como descrição de um painel que não existe mais.
2. O ciano do painel lê **bem mais saturado que o PNG de referência** nesse tamanho. É literalmente «o mesmo azul que fica no card quando da o hover», então foi entregue como pedido — se a intenção era o azul da referência e não o do hover, é uma linha de CSS para trocar.

### Portões

- `npm run lint`: **22 problemas, 0 erros** (22 avisos). Nenhum nos arquivos tocados — a lista é `Consulting`, `ContactModal`, `EventsGrid`, `EventsSpotlight`, `Header`, `MotionPreferenceDialog/Intro`, `RoadmapStopDialog`, `PartnersGrid`, `ProofJourney`, `MorphingHeadline`, `PillarsCarousel`, mais configs e arquivos de `.claude/`.
- Console em movimento normal: nenhum erro. Há um aviso pré-existente do Next sobre `loading="eager"` no `sistran-logo.png` como LCP, alheio a esta volta.
- Movimento reduzido pelas **duas vias**: `@media` → `selo: none`, `orbita: none`, ponto parado; `html[data-motion="reduce"]` → `selo: none`, `orbita: none`.
- Hover: botão avança 6px em normal (`matrix(1,0,0,1,6,−23.76)`) e **não avança** em reduzido (`matrix(1,0,0,1,0,−23.76)`); brilho gira 180° (`matrix(−1,0,0,−1,0,0)`).
- Nó vazado dentro do vidro: folga de 25/30/34/42px a 1024/1280/1440/1920 (antes a conta era medida na janela e ia a **−50px** com a margem nova, jogando o nó para fora do painel; passou a medir da borda do próprio painel).
- Mobile 390: arte volta ao fluxo (`position: relative`, 266×217), balões dentro da seção, `scrollWidth` 390.

Medidas em `docs/medidas/cta-referencia-sis253-depois.json`; capturas em `docs/capturas/sis253-esg-cta-{1024,1280,1920}-v5.png`, `sis253-esg-cta-1440-{hover,reduce}.png` e `sis253-esg-cta-390.png`.

### Continua aberto (da 4ª volta, sem resposta)

1. A seta do botão fica branca a ~2,4:1 sobre o disco ciano (como na referência) ou volta para o navy `#04263f`?
2. O pouso do fio fica no centro do disco ou muda para o nó acima do botão, como no PNG de referência?

### Comentário 7 — `ac6f381c-4682-49d8-b732-a460a9ba9f85`

- **createdAt:** 2026-09-15T17:54:02.435Z
- **updatedAt:** 2026-09-15T17:54:02.405Z
- **parentId:** null
- **resolvedAt:** null
- **quotedText:** null
- **author:** Maria Eduarda M Camargo (`d540a312-a721-479d-9da4-3b86ed7868ba`)
- **onBehalfOf:** null
- **attachments:** []



#### Corpo integral

Feedback 15/09 + print: grafismo da direita (blob/logo) **fica baixo e corta no rodapé** mesmo com o scroll/entrada ok. Nova **SIS-276**.


## Falhas / lacunas

- Comentário ac6f381c-4682-49d8-b732-a460a9ba9f85 menciona print; list_comments.attachments=[] (arquivo não veio na API).
