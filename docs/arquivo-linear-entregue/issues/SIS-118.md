# SIS-118 — /blog e /blog/[slug]: a seção está órfã no site e o post tem três links mortos

## Cabeçalho / data da exportação

```json
{
  "exportadoEm": "2026-09-15T20:50:00.000Z",
  "fonte": "Linear MCP user-linear: get_issue (includeRelations, includeCustomerNeeds, includeReleases = true) + list_comments (limit 250, orderBy createdAt)",
  "modo": "READ-ONLY (get_issue + list_comments; Linear não alterado)",
  "issueId": "SIS-118",
  "uuid": "96c8ec72-3c83-4c08-b5ca-8d75018a8b71",
  "url": "https://linear.app/sistran-labs/issue/SIS-118/blog-e-blogslug-a-secao-esta-orfa-no-site-e-o-post-tem-tres-links",
  "listComments": {
    "issueId": "SIS-118",
    "limit": 250,
    "orderBy": "createdAt",
    "hasNextPage": false,
    "comentariosNoPayload": 2
  }
}
```

## Metadados

Payload integral de `get_issue` (campo `description` omitido aqui; ver seção seguinte). Demais campos preservados.

```json
{
  "id": "SIS-118",
  "uuid": "96c8ec72-3c83-4c08-b5ca-8d75018a8b71",
  "title": "/blog e /blog/[slug]: a seção está órfã no site e o post tem três links mortos",
  "priority": {
    "value": 0,
    "name": "No priority"
  },
  "url": "https://linear.app/sistran-labs/issue/SIS-118/blog-e-blogslug-a-secao-esta-orfa-no-site-e-o-post-tem-tres-links",
  "gitBranchName": "meduardamcp/sis-118-blog-e-blogslug-a-secao-esta-orfa-no-site-e-o-post-tem-tres",
  "createdAt": "2026-09-03T13:56:22.381Z",
  "updatedAt": "2026-09-15T20:34:36.933Z",
  "archivedAt": null,
  "completedAt": null,
  "startedAt": "2026-09-06T16:36:25.972Z",
  "canceledAt": null,
  "dueDate": null,
  "slaStartedAt": null,
  "slaMediumRiskAt": null,
  "slaHighRiskAt": null,
  "slaBreachesAt": null,
  "status": "Entregue",
  "statusType": "started",
  "labels": [
    "conferido"
  ],
  "attachments": [],
  "documents": [],
  "stateHistory": [
    {
      "state": {
        "id": "bc5681b9-61b8-47a5-b698-88ac72996d2e",
        "name": "Backlog",
        "type": "backlog"
      },
      "startedAt": "2026-09-03T13:56:22.381Z",
      "endedAt": "2026-09-06T16:36:25.980Z"
    },
    {
      "state": {
        "id": "5f40aa5d-3589-452e-8bff-8f2539a4fd24",
        "name": "In Progress",
        "type": "started"
      },
      "startedAt": "2026-09-06T16:36:25.980Z",
      "endedAt": "2026-09-06T16:43:27.871Z"
    },
    {
      "state": {
        "id": "c172d2db-acea-4327-b3f6-b72de70efe6a",
        "name": "In Review",
        "type": "started"
      },
      "startedAt": "2026-09-06T16:43:27.871Z",
      "endedAt": "2026-09-15T20:34:36.892Z"
    },
    {
      "state": {
        "id": "dbb9dfbe-2cc4-423a-862f-94ca7705f813",
        "name": "Entregue",
        "type": "started"
      },
      "startedAt": "2026-09-15T20:34:36.892Z",
      "endedAt": null
    }
  ],
  "customerNeeds": [],
  "releases": [],
  "createdBy": "Maria Eduarda M Camargo",
  "createdById": "d540a312-a721-479d-9da4-3b86ed7868ba",
  "team": "Sistran Labs",
  "teamId": "b98f204c-dea3-41e6-99c3-c0347bfb0454",
  "relations": {
    "blocks": [],
    "blockedBy": [],
    "relatedTo": [
      {
        "id": "SIS-120",
        "title": "/solucoes/[slug]: as sete páginas de produto são texto corrido, sem imagem, sem volta e sem navegação entre elas"
      },
      {
        "id": "SIS-266",
        "title": "Footer · item ativo na coluna Navegação conforme a página"
      },
      {
        "id": "SIS-169",
        "title": "Quem somos · Escritórios BRASIL: a rolagem volta a dirigir — transição de Pato Branco para São Paulo entre as duas referências"
      },
      {
        "id": "SIS-125",
        "title": "Varredura por tela: índice das alterações pendentes, rota por rota"
      }
    ],
    "duplicateOf": null
  }
}
```

## Descrição original

## Problema

1. **Ninguém chega ao blog.** Busca em `src/`: a única referência a `/blog` fora da própria rota é `src/app/sitemap.ts:27`. Não há item no menu (`src/data/nav.ts` não tem "Blog"), não há link no rodapé, não há chamada em página nenhuma. A seção existe, é indexável, e é inalcançável navegando.
2. **O post tem três rótulos que não levam a nada.** `src/app/blog/[slug]/page.tsx:51-57` renderiza "ASSISTA A 1ª SESSÃO:", "2ª" e "3ª" como texto puro. O `TODO` na linha 48 registra o motivo: os embeds não existem no projeto. Para o leitor, são três promessas de vídeo sem vídeo — pior que omitir.
3. `generateStaticParams` **cobre dois posts, a página lista dois, e o corpo do post é fixo.** `src/data/blog.ts` tem 2 entradas, mas o corpo do artigo em `[slug]/page.tsx:33-46` é texto **hard-coded** — o mesmo conteúdo é servido para qualquer slug. Abrir o segundo post mostra o texto do primeiro.
4. **A grade é de três colunas para dois itens.** `lg:grid-cols-3` com dois posts deixa um terço vazio.
5. **Sem seções mapeadas.** `/blog` e `/blog/[slug]` não estão em `src/data/pageSections.ts`.

## O que fazer

* Decidir se o blog entra na navegação. Se não entra, decidir se sai do sitemap — publicar no sitemap o que não se navega é escolha, e hoje ela não foi feita conscientemente.
* Tirar o corpo do post do JSX e levá-lo para `src/data/blog.ts`, por slug.
* Resolver os três rótulos de sessão: com as URLs, viram link/player; sem elas, saem.
* Ajustar a grade ao número real de posts.

## Onde

* `src/app/blog/page.tsx` — grade em `lg:grid-cols-3` (linha 29)
* `src/app/blog/[slug]/page.tsx` — corpo fixo (33-46), rótulos sem destino (51-57), `TODO` (48)
* `src/data/blog.ts` — 2 posts, sem campo de corpo
* `src/app/sitemap.ts:27`
* `src/data/pageSections.ts`

## Pontos de atenção

* **O bug do corpo fixo é o mais grave e não é visual:** dois slugs servindo o mesmo artigo é conteúdo errado publicado, não layout torto. Resolver antes do resto.
* Se o corpo virar dado, ele tem marcação (negrito, aspas tipográficas, `&ldquo;`). Definir o formato antes de mover — string com HTML, MDX ou blocos estruturados — porque trocar depois custa mais.
* O comentário na linha 13-17 de `blog/page.tsx` registra que a sidebar de Categorias/Arquivos e o campo de busca foram removidos porque "não há o que pesquisar com um post". Com dois, ainda vale; se o blog voltar a receber conteúdo, isso volta à mesa.
* `<title>` do site é "Blog Grid – Sistran", título de demonstração do tema que ficou indexado (comentário na linha 7). Já corrigido aqui — não regredir.

## Critérios de aceite

- [ ] Cada slug serve o seu próprio corpo de texto
- [ ] Corpo dos posts fora do JSX, em `src/data/blog.ts`
- [ ] Decisão registrada sobre o blog na navegação, e implementada (entra no menu/rodapé, ou sai do sitemap)
- [ ] Os três rótulos de sessão viram link real ou saem
- [ ] Grade sem coluna vazia para o número real de posts
- [ ] Rotas em `pageSections.ts` se ganharem seções
- [ ] Verificado em 1920×1080, 1366×768 e mobile


## Relações / Anexos / Releases / Customer needs

```json
{
  "relations": {
    "blocks": [],
    "blockedBy": [],
    "relatedTo": [
      {
        "id": "SIS-120",
        "title": "/solucoes/[slug]: as sete páginas de produto são texto corrido, sem imagem, sem volta e sem navegação entre elas"
      },
      {
        "id": "SIS-266",
        "title": "Footer · item ativo na coluna Navegação conforme a página"
      },
      {
        "id": "SIS-169",
        "title": "Quem somos · Escritórios BRASIL: a rolagem volta a dirigir — transição de Pato Branco para São Paulo entre as duas referências"
      },
      {
        "id": "SIS-125",
        "title": "Varredura por tela: índice das alterações pendentes, rota por rota"
      }
    ],
    "duplicateOf": null
  },
  "attachments": [],
  "documents": [],
  "releases": [],
  "customerNeeds": []
}
```

### Anexos baixados nesta exportação

```json
[]
```

## Comentários cronológicos

Total no payload: 2. Ordenação: `createdAt` crescente (desempate por `id`).

### Comentário 1 — 2026-09-06T16:43:23.916Z — Maria Eduarda M Camargo

#### Campos / metadados do comentário (exceto corpo)

```json
{
  "id": "bc2cbd8c-b87f-44bf-9846-32118874e607",
  "attachments": [],
  "createdAt": "2026-09-06T16:43:23.916Z",
  "updatedAt": "2026-09-06T16:43:23.890Z",
  "parentId": null,
  "resolvedAt": null,
  "quotedText": null,
  "author": {
    "id": "d540a312-a721-479d-9da4-3b86ed7868ba",
    "name": "Maria Eduarda M Camargo"
  },
  "onBehalfOf": null
}
```

#### Corpo original

## Feito

**Antes de tudo: a premissa dos itens 3 e 4 está errada, e vale corrigir em vez de calar.** A issue diz que `src/data/blog.ts` tem **2 entradas**, que "a página lista dois" e que "abrir o segundo post mostra o texto do primeiro". O arquivo tem **1 post** — `webinar-suitability-e-ai-em-seguros-ruptura-ou-inovacao` — e o comentário de cabeçalho dele já dizia isso ("O blog do site tem um unico post, de 5 de marco de 2024"). Com um slug só, o bug de "dois slugs servindo o mesmo artigo" **não estava visível na tela**: o texto fixo por acaso era o texto do único post.

Isso muda o que o defeito é, não se ele existe. O corpo estava escrito à mão no JSX e o `generateStaticParams` gera **uma rota por slug**: o segundo post a entrar seria publicado com o texto do primeiro, sem nenhum erro de compilação avisando. Corrigido igual, e por isso mesmo — a correção é estrutural, e é a única maneira de o critério "cada slug serve o seu próprio corpo" valer também amanhã.

**1. Corpo do post fora do JSX, por slug.** `Post.body` em `src/data/blog.ts`. O texto foi transportado **sem uma palavra alterada**.

**Formato, decidido antes de mover** (o ponto de atenção da issue): **blocos estruturados tipados**, não string com HTML nem MDX.
- HTML em string exigiria `dangerouslySetInnerHTML` para renderizar o negrito — superfície de injeção num campo de conteúdo para ganhar duas tags.
- MDX custaria dependência, loader e configuração de build para um post.
- Blocos tipados dão erro de compilação quando o dado está errado, atravessam o Server Component sem serialização especial, e crescem por acréscimo: nova variante em `PostBlock` faz o `switch` da página **não compilar** até ser tratada (o `default` tem `never`).

As entidades `&ldquo;`/`&rdquo;` só existiam porque o texto vivia dentro de JSX; em dado viraram o caractere tipográfico direto — conferido no HTML servido (aspas curvas presentes, `&ldquo;` ausente).

**2. Os três rótulos de sessão saíram.** "ASSISTA A 1ª/2ª/3ª SESSÃO:" eram texto puro seguido de nada: promessa de vídeo sem vídeo, não clicável, e em leitor de tela item de lista sem destino. A issue dá as duas saídas ("com as URLs, viram link/player; sem elas, saem") e as URLs não estão no projeto — então saem. O JSX ficou **comentado no lugar**, com o que fazer quando as três URLs chegarem. Conferido: `ASSISTA` aparece **0 vezes** no HTML servido, e o `<article>` tem 1 `<p>` e 0 `<li>`.

**3. O blog entra na navegação — no rodapé, coluna "Institucional".** Das duas saídas oferecidas, esta não joga conteúdo real fora: tirar `/blog` do sitemap resolveria a incoerência apagando o lado certo dela. Rodapé e não menu porque as sete entradas do topo carregam a oferta, e um blog com **uma** publicação de 2024 ao lado delas anuncia uma frequência que não existe; o rodapé é onde o site já põe o institucional de baixa frequência. Ficou fora da coluna "Navegação" de propósito: aquela é gerada de `NAV_ITEMS` e um item que o header não tem faria as duas listas divergirem. `sitemap.ts` intacto.

**4. Grade sem coluna vazia.** Era `lg:grid-cols-3` com um post — um cartão e dois terços de largura vazios. As colunas agora saem de `POSTS.length` (1 → uma coluna com `max-w-md`, 2 → duas, 3+ → três), para o layout não voltar a descrever um blog que não existe. `max-w-md` porque um cartão único sem limite esticaria a caixa de texto por 1920px. Medido: 448px em 1920 e 1366, 350px em 390.

**5. `pageSections.ts` — sem alteração, e é o certo.** `/blog` tem duas seções (abertura + publicações) e o critério dos 3 do cabeçalho daquele arquivo já a exclui nominalmente; `/blog/[slug]` é rota dinâmica de página única. O critério de aceite é condicional ("se ganharem seções") e elas não ganharam.

**6. `<title>` não regrediu:** segue `Blog · Sistran`, com o comentário sobre o "Blog Grid – Sistran" no lugar.

## Portões

- `npx tsc --noEmit` limpo
- `npx eslint src --ext .ts,.tsx` → 18 avisos / 0 erros (a linha de base, todos `react-hooks/static-components`)
- `npx next build` exit 0
- `curl` 200 em `/blog`, `/blog/<slug>` e `/`
- Medido em **1920×1080, 1366×768 e 390×844**: transbordo horizontal **0** nas 6 medições, 1 `<h1>` por página, link do rodapé presente nas seis

## Parado, para quem responde pelo conteúdo

**As três URLs dos vídeos do ciclo de webinars (25/11, 02/12, 09/12).** Com elas, os três rótulos voltam como link/player — o JSX está comentado no lugar exato. Sem elas não há como pôr destino sem inventar. Não bloqueou nada do resto.

### Comentário 2 — 2026-09-08T14:44:39.222Z — Maria Eduarda M Camargo

#### Campos / metadados do comentário (exceto corpo)

```json
{
  "id": "5ad094de-494d-4fda-86a6-c25bcd4cf70a",
  "attachments": [],
  "createdAt": "2026-09-08T14:44:39.222Z",
  "updatedAt": "2026-09-08T14:44:39.192Z",
  "parentId": null,
  "resolvedAt": null,
  "quotedText": null,
  "author": {
    "id": "d540a312-a721-479d-9da4-3b86ed7868ba",
    "name": "Maria Eduarda M Camargo"
  },
  "onBehalfOf": null
}
```

#### Corpo original

## Conferido — aprovado

Confiro os seis critérios contra o código e contra medição própria. Todos cumpridos, e o defeito mais grave foi resolvido pela raiz e não pelo sintoma.

**Antes de tudo, a correção de premissa está certa e eu confirmo.** `src/data/blog.ts` tem **um** post (`webinar-suitability-e-ai-em-seguros-ruptura-ou-inovacao`), não dois — a issue que eu escrevi errou aí. E a leitura de que isso muda *o que o defeito é*, não *se ele existe*, é a leitura correta: com um slug só o texto fixo no JSX coincidia com o texto do post, e o `generateStaticParams` gera uma rota por slug, então o segundo post entraria publicado com o corpo do primeiro sem um único erro de compilação. Corrigir estruturalmente era a única forma de o critério valer amanhã. Contrariar a issue com prova é o comportamento certo.

**Corpo por slug, fora do JSX.** `Post.body` em `blog.ts:41`; a página não contém mais texto de artigo — só `post.body.map` (`[slug]/page.tsx:36`). Conferi o transporte palavra por palavra contra o texto anterior: nada alterado. O `<strong>` renderizado traz exatamente os dois trechos que o post publicado tinha em negrito ("ciclo de webinars nos dias 25 de novembro, 2 e 9 de dezembro" e "Sistran Informática"). `&ldquo;` **ausente** do HTML servido e aspas curvas **presentes**, nas três telas.

**O formato foi escolhido antes de mover, que era o ponto de atenção da issue.** Blocos tipados em vez de string com HTML ou MDX, com os três motivos escritos em `blog.ts:5-19`. O argumento do `dangerouslySetInnerHTML` procede — abrir injeção num campo de conteúdo para ganhar duas tags é troca ruim. E o `default: { const naoTratado: never = bloco.kind }` de `[slug]/page.tsx:100-103` é o que dá dente à escolha: a variante nova não compila até ser desenhada, em vez de sumir da tela. Verifiquei que o `key` vem do conteúdo e não do índice, coerente com o `chaveDoBloco` de `acceleratorPages.ts`.

**Os três rótulos saíram, e saíram comentados no lugar** (`[slug]/page.tsx:40-60`), com a instrução do que fazer quando as URLs chegarem — regra da casa cumprida. `grep -rn ASSISTA src/` devolve os dois hits legítimos de `EventsSpotlight.tsx` e, no blog, **só dentro do comentário**. No HTML servido: `ASSISTA` 0 vezes, `<article>` com 1 `<p>` e **0 `<li>`**, nas três telas.

**Blog na navegação — decisão registrada e implementada.** Rodapé, coluna "Institucional" (`Footer.tsx:123-143`), `sitemap.ts` intacto. O raciocínio de que sair do sitemap "resolveria a incoerência apagando o lado certo dela" é o certo, e ficar fora da coluna "Navegação" porque ela é gerada de `NAV_ITEMS` evita criar a segunda lista divergente. Link presente no rodapé nas **seis** medições.

**Grade sem coluna vazia.** Medido no `gridTemplateColumns` computado: **448px** (uma coluna, `max-w-md`) a 1920 e a 1366; **350px** a 390. Um cartão, uma coluna. Derivar de `POSTS.length` em vez de fixar é melhor que o pedido — o layout deixa de poder descrever um blog que não existe.

**`pageSections.ts` sem alteração, e concordo.** `/blog` tem duas seções e o critério dos 3 do cabeçalho daquele arquivo a exclui nominalmente (linha 19); o critério de aceite era condicional e a condição não se realizou. Não é omissão.

**Portões — refiz com o portão certo.** `npx tsc --noEmit` limpo. `npm run lint` → **24 avisos / 0 erros**, a linha de base, e filtrando a saída por `blog` e `Footer` não sai **nada**: nenhum aviso vem dos arquivos tocados. Extra: `/blog/nao-existe` devolve **404**, então o `notFound()` cobre slug inválido. Transbordo horizontal **0** e exatamente **1 `<h1>`** nas seis medições.

## Sugestões — nenhuma bloqueia

**1. O post é ponto sem saída.** Medido: `main a[href="/blog"]` **não existe** na página do post, nas três telas. A issue tratou de como se *chega* ao blog e isso está resolvido, mas quem cai no post direto (ele está no sitemap, `sitemap.ts:28`, e é justamente a página que se alcança por busca) não tem caminho para a listagem — só o CTA de contato. Um "voltar para o blog" no fim do `<article>`, no molde do `<Link href="/solucoes">` que a SIS-120 pôs no fim das páginas de acelerador, fecha o circuito. Fora do escopo dos critérios; vale como item próprio ou como carona na próxima issue de blog.

**2. Comentário que envelheceu em `pageSections.ts:21-22`.** A linha diz que as rotas dinâmicas "(`/blog/[slug]`, `/solucoes/[slug]`) nem chegam a ter chave aqui". Vale para `/blog/[slug]`, mas deixou de valer para `/solucoes/[slug]`, que agora tem paradas derivadas na linha 208. Não é desta issue — é resíduo da SIS-120, e vou apontar lá. Registro aqui porque foi esta conferência que o encontrou, e porque é justamente o critério que a SIS-118 cita para se justificar.

**3. Uma frase do cabeçalho de `blog/page.tsx:13-17` descreve o que não existe mais.** "A grade tem o unico post que o site publica" segue verdadeiro, mas o texto sobre a sidebar reduzida "a categoria do post, sem campo de busca" fala de uma sidebar que não está no JSX — a categoria hoje aparece dentro do cartão. Uma linha só, mas é o tipo de nota que na próxima leitura manda alguém procurar um componente que não existe.

## Ponto parado, encaminhado

As **três URLs dos vídeos do ciclo de webinars (25/11, 02/12, 09/12)**. Registro corretamente como informação que não está no projeto, com o JSX comentado no lugar exato para o retorno. Nada mais dependia disso.

Removo `conferir`, aplico `conferido`, mantenho em **In Review**.

## Falhas / lacunas

```json
[]
```
